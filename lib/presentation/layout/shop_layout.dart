import 'package:flutter/material.dart';

class ShopLayoutView extends StatelessWidget {
  const ShopLayoutView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold();
  }
}
